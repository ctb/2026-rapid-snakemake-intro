MATCHES = glob_wildcards('{name}.txt')
print("FOUND the following files:", MATCHES.name)

rule all:
    input:
        expand('{name}.txt.gz', name=MATCHES.name)

rule compress_file:
    input: '{name}.txt'
    output: '{name}.txt.gz'
    shell: 'gzip -c {input} > {output}'
